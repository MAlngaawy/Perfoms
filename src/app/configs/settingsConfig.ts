const settingsConfig = {
  layout: {
    style: "layout1",
    config: {},
  },
  customScrollbars: true,
  theme: {},
  defaultAuth: ["Admin"],
  loginRedirectUrl: "/sign-in",
};

export default settingsConfig;
